{
  "schema": "migo-v8-build-lock/v1",
  "notes": [
    "The source half of the macOS V8 supply chain. It pins IDENTITY -- which",
    "rusty_v8, which V8, which gn, which targets -- and deliberately does not pin",
    "gn arguments. android-v8.lock.json and ohos-v8.lock.json do not either: the",
    "argument list is exported by the build recipe and recorded into each",
    "component-manifest.json's `normalized_gn_args` by the manifest writer, so it",
    "is a description of what a real build used rather than a claim typed here in",
    "advance. windows-v8.lock.json is the exception and says why in its own notes:",
    "that target cross-compiles to a DLL and pins the arguments because the shape",
    "of the artifact depends on them.",
    "",
    "WHAT IS DELIBERATELY ABSENT: hashes. A component-manifest.json carries the",
    "sha256 of real bytes, and those bytes do not exist until a macOS runner has",
    "built them -- .github/workflows/apple-v8-probe.yml exists to establish what",
    "that build costs and which arguments it accepts before a recipe is written",
    "against guesses. The Apple ANGLE round made the price of skipping that step",
    "explicit: its probe ran first, the recipe was written against measured facts,",
    "and the round still needed four lane iterations, every one of which found a",
    "defect only a real build could reveal.",
    "",
    "iOS is not here, and that is a decision rather than an omission. On iOS the",
    "content JavaScript runs in WKWebView's WebContent process to get the system",
    "JIT -- an embedded V8 there would be interpreted, which is the arrangement",
    "the project already rejected for in-process JavaScriptCore. macOS is the one",
    "Apple platform with no architectural obstacle to in-process V8: the public",
    "hardened-runtime entitlement allows JIT.",
    "",
    "MEASURED 2026-09-09 by .github/workflows/apple-v8-probe.yml, run 34299937361,",
    "on macos-15 (Apple silicon). The five questions that lane existed to answer,",
    "answered -- so what follows is a record of a real build rather than a plausible",
    "argument list, which is the whole reason the probe ran before this file grew a",
    "recipe. See `measured` below for the numbers."
  ],
  "rusty_v8_version": "145.0.0",
  "rusty_v8_revision": "e6a88b35dd3d7f2849a0df33a71d338701c55316",
  "v8_revision": "8defb67673c5483ae56258a2de01b07e947dc921",
  "sources": {
    "rusty_v8": "https://github.com/denoland/rusty_v8/tree/e6a88b35dd3d7f2849a0df33a71d338701c55316",
    "v8": "https://chromium.googlesource.com/v8/v8/+/8defb67673c5483ae56258a2de01b07e947dc921"
  },
  "gn": {
    "version": "2502",
    "revision": "17b0057970fa2b07a20cbb4289ab78cf93565f35",
    "source": "https://gn.googlesource.com/gn",
    "required_patches": [
      "0001-suppress-gcc-multiline-comment-warning"
    ],
    "notes": [
      "The same gn source, revision and patch set the Android and OpenHarmony",
      "builds pin. The BINARY cannot be shared with them -- theirs is a Linux",
      "executable and this recipe runs on macOS -- so what is pinned here is the",
      "source identity the macOS binary must also have been built from.",
      "",
      "The patch is named for gcc and is still listed for macOS on purpose: it is",
      "part of the pinned gn identity, and a gn built from a different patch set is",
      "a different gn no matter which compiler ignored the warning it suppresses.",
      "Whether Apple's clang needs the suppression is not the question; whether",
      "this is the same gn is."
    ]
  },
  "required_patches": [],
  "macos": {
    "deployment_target": "11.0",
    "notes": [
      "Derived from contracts/apple/deployment-floor.json's macos platform entry,",
      "not chosen here. That file is the single source for every Apple deployment",
      "target and scripts/test-apple-deployment-floor-contract.sh fails the build",
      "when a consumer carries its own copy of the number -- this entry exists so",
      "the V8 recipe has something to pass to the compiler, and it is checked",
      "against the contract rather than trusted.",
      "",
      "It matters for a reason with no build step to land in: an archive built",
      "without an explicit deployment target inherits the runner's own macOS",
      "version as its floor. That links on the runner and fails on a consumer's",
      "older Mac, which is the same shape as the ANGLE loader-path trap -- correct",
      "everywhere the project can see, wrong where it cannot."
    ]
  },
  "targets": {
    "aarch64": {
      "triple": "aarch64-apple-darwin",
      "cpu_baseline": "armv8-a",
      "required_cpu_features": [
        "neon"
      ]
    },
    "x86_64": {
      "triple": "x86_64-apple-darwin",
      "cpu_baseline": "x86-64-v1",
      "required_cpu_features": [
        "cmov",
        "sse2"
      ]
    }
  },
  "targets_notes": [
    "The baselines are the same conservative pair every other platform's lock",
    "carries, and that is deliberate. This field states the MINIMUM a consumer's",
    "CPU must satisfy, not a description of the machine that built the archive.",
    "",
    "It would be easy to write something higher and plausible here: macOS 11 runs",
    "on Apple silicon from the M1 up and on Intel Macs from roughly Haswell, so",
    "every supported machine does in fact have NEON, SSE4.2 and POPCNT. But",
    "whether the ARCHIVE requires them is a question about what the compiler",
    "emitted, and nothing has compiled it yet. Raising a baseline is a claim about",
    "bytes; it belongs in the same change as the bytes."
  ],
  "measured": {
    "probe_run": "https://github.com/minigame-labs/migo-runtime/actions/runs/34299937361",
    "measured_on": "2026-09-09",
    "runner": "macos-15, Apple silicon",
    "_comment": [
      "Five answers, and one of them changes the recipe.",
      "",
      "1. V8_FROM_SOURCE works on a hosted macOS runner. The host triple built in",
      "   57 minutes and left 37 GiB free on a 320 GiB volume, so disk -- the thing",
      "   the ANGLE round had to correct an inherited assumption about -- is not the",
      "   constraint here either.",
      "",
      "2. x86_64-apple-darwin cross-builds as a first-class case: a plain",
      "   `cargo build --release --target x86_64-apple-darwin -p v8` with no extra",
      "   arguments and no pinned v8_snapshot_toolchain. OpenHarmony needed one and",
      "   its recipe has two code paths because of it; darwin needs one.",
      "",
      "3. src_binding.rs IS generated here (870 lines). ohos-v8.lock.json records a",
      "   pre-committed binding as structural because that SDK's clang is too old for",
      "   bindgen; Xcode's is not, so darwin has no such exception.",
      "",
      "4. gn_args_measured below is the verbatim args.gn the accepted build used,",
      "   minus clang_base_path, which names a path inside the runner's temp",
      "   directory and is machine-local by construction.",
      "",
      "5. THE ONE THAT CHANGES THE RECIPE: the archive carries `minos 12.0`, and",
      "   this file's own macos.deployment_target is 11.0. Left alone the build",
      "   inherits whatever the runner's SDK defaults to -- 12.0 on this image, with",
      "   an SDK of 15.5 -- so a macOS product linking it would fail to load on the",
      "   macOS 11 the deployment floor declares, and the floor would be a claim the",
      "   product does not meet. It is exactly the shape of the ANGLE loader-path",
      "   trap: correct on the machine that built it, wrong on a consumer's. The",
      "   recipe must PASS the deployment target explicitly and a gate must check",
      "   what the bytes say, not what the recipe intended."
    ],
    "host_build_seconds": 3439,
    "cross_build_seconds": 3356,
    "job_wall_clock_minutes": 115,
    "job_timeout_minutes": 120,
    "_budget_comment": [
      "115 of a 120-minute cap, for two builds and nothing else -- no archiving, no",
      "hashing, no upload. So the shipping recipe cannot put both triples in one job",
      "the way this probe did. One triple per job, and the assembly that joins them",
      "is a third. That is a conclusion about the LANE, not about the build, and it",
      "is recorded here because the next person to write the lane will otherwise",
      "copy the probe's shape and discover the cap with a green-looking cancellation."
    ],
    "host_archive_bytes": 131862848,
    "src_binding_generated": true,
    "src_binding_lines": 870,
    "mach_o_minos_observed": "12.0",
    "mach_o_sdk_observed": "15.5",
    "gn_args_measured": [
      "is_debug = false",
      "use_custom_libcxx = true",
      "v8_enable_sandbox = false",
      "v8_enable_pointer_compression = false",
      "v8_enable_v8_checks = false",
      "host_cpu = \"arm64\"",
      "target_cpu = \"arm64\""
    ],
    "gn_args_excluded": {
      "clang_base_path": "Names a path inside the runner's temp directory. Machine-local, so it is not part of what a pin can assert."
    }
  }
}
