# Astro / 构建产物:`pnpm docs:build` 全部重新生成,不入库。(`node_modules/` 由仓库
# 根 .gitignore 覆盖,不再重复。)
build/
.astro/

# Playwright(docs:smoke)产物:截图回放、报错现场、浏览器下载缓存。
playwright-report/
test-results/
playwright/.cache/